@model IEnumerable<Desafio.Models.Starter>
@{
    ViewData["Title"] = "Starters";
}
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0">Lista de starters</h1>
          </div><!-- /.col -->
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->
        <table id="Programas" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>4 Letras</th>
                    <th>Nome</th>
                    <th>Tecnologia</th>
                    <th>Turma</th>
                    <th>Açoes</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var starter in Model)
                {
                    <tr>
                        <td>@starter.Id</td>
                        <td>@starter.Letras</td>
                        <td>@starter.Nome</td>
                        <td>@starter.Tecnologia.Tipo</td>
                        <td>@starter.Programa.Id</td>
                        <td>
                            <a class="btn btn-primary" href="../Adm/EditarStarter/@starter.Id">Editar</a>
                            <form asp-controller="Starter" asp-action="Deletar" style="display: inline;">
                                <input type="hidden" name="id" id="id" value="@starter.Id">
                                <button class="btn btn-danger">Deletar</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    <hr>
    <a asp-controller="Adm" asp-action="NovoStarter" class="btn btn-success">Novo Starter</a>
    <!-- Main content -->
    <div class="content">
      <div class="container-fluid">
        
      </div><!-- /.container-fluid -->
    </div>
    <!-- /.content -->
  </div>
  <!-- /.content-wrapper -->

  <!-- Control Sidebar -->
  <aside class="control-sidebar control-sidebar-dark">
    <!-- Control sidebar content goes here -->
    <div class="p-3">
      <h5>Title</h5>
      <p>Sidebar content</p>
    </div>
  </aside>
  <!-- /.control-sidebar -->
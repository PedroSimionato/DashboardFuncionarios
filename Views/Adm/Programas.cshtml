@model IEnumerable<Desafio.Models.ProgramaStarter>
@{
    ViewData["Title"] = "Programas";
}
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0">Lista de programas Starter</h1>
          </div><!-- /.col -->
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->
        <table id="Programas" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Turma</th>
                    <th>Inicio</th>
                    <th>Fim</th>
                    <th>Açoes</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var prog in Model)
                {
                    <tr>
                        <td>@prog.Id</td>
                        <td>@prog.Turma</td>
                        <td>@prog.Inicio.ToString("dd/MM/yyyy")</td>
                        <td>@prog.Fim.ToString("dd/MM/yyyy")</td>
                        <td>
                            <a class="btn btn-primary" href="../Adm/EditarPrograma/@prog.Id">Editar</a>
                            <form asp-controller="Programa" asp-action="Deletar" style="display: inline;">
                                <input type="hidden" name="id" id="id" value="@prog.Id">
                                <button class="btn btn-danger">Deletar</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    <hr>
    <a asp-controller="Adm" asp-action="NovoPrograma" class="btn btn-success">Novo Programa</a>
    <!-- Main content -->
    <div class="content">
      <div class="container-fluid">
        
      </div><!-- /.container-fluid -->
    </div>
    <!-- /.content -->
  </div>
  <!-- /.content-wrapper -->

  <!-- Control Sidebar -->
  <aside class="control-sidebar control-sidebar-dark">
    <!-- Control sidebar content goes here -->
    <div class="p-3">
      <h5>Title</h5>
      <p>Sidebar content</p>
    </div>
  </aside>
  <!-- /.control-sidebar -->